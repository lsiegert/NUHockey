<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.lsiegert"
      android:versionCode="1"
      android:versionName="1">
    <application android:icon="@drawable/icon_terrible" android:label="@string/app_name" android:debuggable="true">
        <activity android:name=".NUHockey"
                  android:label="@string/app_name"
                  android:theme="@android:style/Theme.Black">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RosterActivity"
        		  android:label="@string/roster"></activity>
        <activity android:name=".ScheduleActivity"
        		  android:label="@string/schedule"></activity>
        <activity android:name=".RSSActivity"
        		  android:label="@string/rss"></activity>
        <activity android:name=".StatsActivity"
        		  android:label="@string/stats"></activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />     
</manifest> 